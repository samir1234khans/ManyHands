{
  "name": "manyhands",
  "version": "0.0.0",
  "private": true,
  "description": "Problem-first coordination for ambitious open-source software.",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24.19.0 <25",
    "pnpm": "11.20.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samir1234khans/ManyHands.git"
  },
  "scripts": {
    "dev": "pnpm --filter @manyhands/web dev",
    "build": "pnpm --filter @manyhands/web build",
    "start": "pnpm --filter @manyhands/web start",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.tests.json eslint.config.mjs prettier.config.mjs playwright.config.ts vitest.config.ts .github/dependabot.yml .github/workflows/application-ci.yml .github/workflows/repository-health.yml apps/web tests",
    "format:check": "prettier --check package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.tests.json eslint.config.mjs prettier.config.mjs playwright.config.ts vitest.config.ts .github/dependabot.yml .github/workflows/application-ci.yml .github/workflows/repository-health.yml apps/web tests",
    "typecheck": "tsc -p apps/web/tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "verify:full": "pnpm verify && pnpm test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "eslint": "9.39.4",
    "eslint-config-next": "16.3.3",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "vitest": "4.1.0"
  }
}
