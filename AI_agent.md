# AI agent instructions

The canonical ManyHands AI and collaborator instructions are maintained in [`AGENTS.md`](AGENTS.md).

Do not duplicate status here. Update `AGENTS.md` and the matching file under `docs/agent-status/` so humans and tools read one source instead of two drifting copies.
